# Database Configuration
# 
# OPTION 1: Connection String (Recommended)
# Use a single connection string for PostgreSQL or MySQL
#
# PostgreSQL examples:
# DB_CONNECTION_STRING=postgresql://user:password@localhost:5432/database_name
# DB_CONNECTION_STRING=postgresql://user:password@localhost:5432/database_name?sslmode=require
#
# MySQL examples:  
# DB_CONNECTION_STRING=mysql://user:password@localhost:3306/database_name
# DB_CONNECTION_STRING=mysql://user:password@localhost:3306/database_name?sslmode=require
DB_CONNECTION_STRING=postgresql://myuser:mypassword@localhost:5432/myapp

# OPTION 2: Individual Parameters (Legacy - for backwards compatibility)
# If you prefer to use individual environment variables instead of a connection string,
# you can still use the original format:
# DB_TYPE=mysql
# DB_HOST=localhost
# DB_PORT=3306
# DB_NAME=myapp
# DB_USER=myuser
# DB_PASSWORD=mypassword
# DB_SSL_MODE=prefer              # SSL mode: none, prefer, require

# Connection Pool Settings (applies to both connection methods)
DB_MAX_CONNS=10                 # Maximum number of open connections
DB_MAX_IDLE_CONNS=5             # Maximum number of idle connections

# Database Access Control (Optional)
# If DB_ALLOWED_NAMES is empty or not set, only the primary database is accessible
# If DB_ALLOWED_NAMES is set, the primary database plus listed databases are accessible
# DB_ALLOWED_NAMES=testdb,devdb,staging    # Comma-separated list of additional allowed databases


